<h1>新規投稿</h1>
<!-- url:どのURLへフォームの情報を送信するか, method:HTTPメソッドを指定 -->
<%= form_with model: @list, url: '/lists', method: :post do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>内容</h4>
  <%= f.text_area :body %>

  <h4>画像</h4>
  <!-- titleやbodyのようにimageにもフィールドを追加(accept:画像ファイルのみ投稿可) -->
  <%= f.file_field :image, accept: "image/*" %>

  <%= f.submit '投稿' %>

<% end %>